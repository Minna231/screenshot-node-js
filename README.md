🚀 Schritt 1: Projektinitialisierung und Puppeteer-Installation
Stellen Sie sicher, dass Node.js installiert ist. Erstellen Sie dann einen neuen Projektordner und installieren Sie Puppeteer.

Ordner erstellen und initialisieren:

mkdir node-screenshot
cd node-screenshot
npm init -y
Puppeteer installieren:

npm install puppeteer
💻 Schritt 2: Node.js-Code für den Screenshot
Erstellen Sie eine neue Datei namens screenshot.js und fügen Sie den folgenden Code ein. Dieser Code startet einen Browser, navigiert zur gewünschten URL, wartet auf das Laden der Seite und erstellt dann den Screenshot.

▶️ Schritt 3: Code ausführen
Führen Sie Ihr Node.js-Skript über das Terminal aus:
node screenshot.js

Wenn Sie mehr erfahren möchten, besuchen Sie https://igas-berlin.de/
